# The content-pack outline and continuity prompts are intentionally long-form.
# Ollama defaults to 4096 tokens, which truncates a 3-day outline before Day 3.
# Keep this override host-local and independent from the Swartzit web bind.
[Service]
Environment="OLLAMA_CONTEXT_LENGTH=16384"
